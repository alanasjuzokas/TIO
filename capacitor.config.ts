import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.domain.tryitout',
  appName: 'TryItOut',
  webDir: 'dist', // Ensure this is set to 'dist' as per Vite's output
};

export default config;
